{
  "name": "chemclaw",
  "version": "1.0.0-claw",
  "private": true,
  "description": "ChemClaw Claw Code — autonomous chemistry knowledge-intelligence agent (v1.0.0-claw)",
  "license": "MIT",
  "workspaces": [
    "services/agent-claw",
    "services/paperclip"
  ],
  "scripts": {
    "build": "npm run build --workspaces --if-present",
    "dev:agent": "npm run dev --workspace services/agent-claw",
    "lint": "npm run lint --workspaces --if-present",
    "typecheck": "npm run typecheck --workspaces --if-present",
    "test": "npm run test --workspaces --if-present",
    "coverage": "npm run coverage --workspaces --if-present"
  },
  "overrides": {
    "uuid": "^14.0.0"
  },
  "engines": {
    "node": ">=22"
  }
}
