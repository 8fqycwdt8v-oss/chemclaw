-c ../../_constraints.txt

# Shared FastAPI helpers (create_app, auth middleware, payload caps).
# Per-service requirements.txt files pull this in transitively via their
# fastapi/uvicorn/httpx pins; declared explicitly here so the CI test job
# (which doesn't install any individual mcp_tools service) can resolve
# the imports in services/mcp_tools/common/tests/.
fastapi>=0.115,<0.120
httpx>=0.27,<0.30
