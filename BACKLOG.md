# BACKLOG

Deferred follow-ups discovered while doing other work. One bullet per item, prefixed with the area in brackets. Append-only; flat list. Don't write justifications — just enough text to rediscover the idea. See `CLAUDE.md` "General rules" #5.

- [ci/projectors] restore `compound_classifier` + `compound_fingerprinter` to the mypy clean subset (currently dropped in the per-package list with a note in `.github/workflows/ci.yml`); pay down the bare `psycopg.AsyncConnection` annotations
- [redactor] org-scoped `redaction_patterns` rows are stored but not yet applied per-call — LiteLLM gateway needs a way to receive the caller's org context (custom HTTP header from agent-claw → callback)
- [agent-claw/llm] migrate per-role inference params (`temperature`, `max_tokens`, `top_p` for planner/executor/compactor/judge) to `config_settings` — needs threading user/project/org context through the `LlmProvider` interface to `litellm-provider.ts`
- [optimizer/reanimator] migrate stalled-definition knobs (`STALE_AFTER_SECONDS`, finish-reason allow-list) from env vars to `config_settings`
- [infra/backups] live end-to-end backup → restore drill in staging (requires real S3 bucket + age key) — `make backup.test-restore` script is in place, the secret + bucket are not
- [ci/workflows] adding tests for a new MCP service requires three CI edits (install reqs, pytest path, sometimes diff-cover exclude) — add a generator or `make ci.regenerate` that derives those from the on-disk service tree, or document the checklist
- [ci/security-hook] `.github/workflows/*.yml` Edit/Write is blocked by the security_reminder_hook even for safe edits — agents currently route around with Python `pathlib.write_text`. Either trust the hook for trusted authors or document the workaround
- [agent-claw/builtins] `design_plate.annotate_yield` always falls back to the global yield model — plumb `model_id` (or `nce_project_internal_id` + `train→predict`) through `design_plate.ts` input so per-project data is used; also drop the inline duplicate `YieldPrediction` Zod schema in favor of importing `PredictYieldWithUqOut`
- [agent-claw/builtins] Z-phase builtins (`recommend_conditions`, `start_optimization_campaign`, `recommend_next_batch`, `ingest_campaign_results`, `extract_pareto_front`) call `postJson` without `{ userEntraId: ctx.userEntraId }` opts — production works via AsyncLocalStorage but unit tests don't exercise the auth path; align with `canonicalize_smiles.ts`
- [agent-claw/builtins] `recommend_next_batch` reads campaign+rounds in txn-1, calls MCP, writes round in txn-2 — `ingest_campaign_results` can land between, leaving the surrogate trained on stale snapshot; add etag read-and-check across the two transactions
- [db/migrations] `optimization_campaigns.nce_project_id NOT NULL` only applies to fresh `CREATE TABLE`; if an environment has the pre-fix nullable column, an explicit `ALTER TABLE ... ALTER COLUMN ... SET NOT NULL` migration with prior backfill is needed (no prod data exists yet, so deferred)
- [mcp_tools/yield_baseline] add unit test for `_domain_dump → _domain_load` round-trip independent of running BoFire's full HTTP path (locks the contract regardless of installed BoFire version)
- [mcp_tools/reaction_optimizer] add unit test exercising `used_bo=True` on a multi-objective campaign; `qNEHVI` source matching is now correct but unverified by tests
- [mcp_tools/yield_baseline] add unit test for `combine_batch` length-mismatch path (mcp-chemprop returns fewer predictions than input list with 200 status) — failure mode reaches a 400 today but is unverified
- [tests/integration] add concurrent-`recommend_next_batch` test asserting only one round row is inserted and the loser raises `round_index_conflict` — locks in the ON-CONFLICT race fix
- [ci/mypy] restore `mcp_doc_fetcher` to mypy clean subset — ~10 errors (unused PyPDF2/pdf2image `type: ignore`, missing `dict[]` generics, `JSONResponse` return-type mismatches in FastAPI handlers)
- [ci/mypy] restore `mcp_eln_local` to mypy clean subset — ~11 errors in `routes.py` (untyped `_acquire`, Row vs dict argument types, missing parameter annotations)
- [ci/mypy] restore `ingestion/doc_ingester` to mypy clean subset — 2 untyped-decorator errors on `typer @app.command()` in `cli.py`
- [ci/mypy] drop the narrow `# type: ignore[no-untyped-call]` on `services/mcp_tools/mcp_xtb/_shared.py:101` (`MolToInchiKey`) once RDKit ships type stubs or a partial stub package is added
- [ci/diff-cover] add real tests for `services/queue/**` (worker LISTEN setup currently 0% covered; excluded from diff-cover in `.github/workflows/ci.yml`)
- [ci/diff-cover] add tests covering `services/optimizer/session_reanimator/**`, `services/projectors/kg_hypotheses/**`, `services/mcp_tools/mcp_drfp/**`, `services/mcp_tools/mcp_rdkit/**` so the diff-cover excludes can be removed
- [cleanup] decide fate of `services/ingestion/eln_json_importer.legacy/**` — currently excluded from diff-cover and ruff; either delete or re-enable
- [parity/v1.4] implement Setting sources (user/project/local) — Claude SDK primitive, currently `not implemented` in `docs/PARITY.md`
- [parity/v1.4] implement ToolSearch (lazy tool loading) — Claude SDK primitive, currently `not implemented` in `docs/PARITY.md`
- [parity/permissions] make `allowedTools` / `disallowedTools` filter the catalog **before** the LLM sees it — current behaviour is a post-call short-circuit via synthetic `denied_by_permissions` tool result
- [parity/security] wire callers for `assertWithinWorkspace` (`services/agent-claw/src/security/workspace-boundary.ts`) when filesystem-shaped tools land — helper exists with tests but has no production caller today
- [DONE cluster-F] hook built-in stubs landed for all 9 dispatched-only lifecycle points (`session_end`, `user_prompt_submit`, `post_tool_failure`, `post_tool_batch`, `subagent_start`, `subagent_stop`, `task_created`, `task_completed`, `post_compact`) via `lifecycle-telemetry.ts` + 9 yaml files. Each emits a structured log line bound to component `lifecycle-telemetry` and returns `{}`. `MIN_EXPECTED_HOOKS` 11→20.
- [tests/coverage] backfill ≥30% line coverage across all services (PR-1 plan called this "out of scope this session"; `docs/review/2026-04-29-codebase-audit/05-coverage-baseline.md` has the per-service table)
- [db/migrations] replace the idempotent-init-files pattern with a real migration system (Alembic or sqitch) — current `db/init/N_*.sql` lex-order replay is fixed but is not a migration tool
- [db/types] replace CHECK constraints with native PostgreSQL ENUM types where the value set is closed (cosmetic; raised in audit)
- [db/audit] add canonical-table audit history (parallel `*_history` tables or temporal tables) — flagged in audit, deferred from PR-1

- [tests/integration] `tests/integration/test_hypotheses_schema.py:204,230` use `DELETE FROM hypotheses` — line 204 is legitimate teardown and line 230 deliberately tests CASCADE FK behaviour, so neither is actually anti-pattern; revisit if we ever want a separate test that exercises the bi-temporal `UPDATE … SET refuted_at = NOW()` pattern
- [tests/integration] add a Neo4j+Postgres round-trip test for `update_hypothesis_status` → `trg_hypotheses_status_event` → `ingestion_events` → `kg_hypotheses` projector → Neo4j node `valid_to` set; needs the existing testcontainer harness expanded to include 03_hypotheses.sql + 35_event_type_vocabulary.sql + a Neo4j container
- [agent-claw/confidence] full auto-trigger of compute_confidence_ensemble on every "substantial" output (post_tool or post_turn hook) is deferred from Tranche 2; M2 currently lands the infrastructure (cross-model now wired through the LlmProvider when the caller opts in) but the trigger gating + per-skill toggle still need design
- [projectors/qm_kg] after `db/init/37_qm_ingestion_events.sql` has been applied to prod for one release, remove (a) the legacy `pg_notify('qm_job_succeeded', ...)` channel from the trigger, and (b) the defensive `INSERT INTO ingestion_events ... ON CONFLICT DO NOTHING` fallback in `services/projectors/qm_kg/main.py:_ack` (the trigger now writes the canonical row, so the fallback is dead weight on a properly-migrated DB)
- [agent-claw/retrieve] full four-mode hybrid retrieval (text → compound + reaction + document + KG with auto-classification) — Tranche 3 ships only the chunk+fact arms via retrieve_related; full mode requires entity resolution from arbitrary text + a query-classification layer
- [agent-claw/maturity] move maturity gating from assertion-time (foundation-citation-guard hook) to retrieval-time — Tranche 5 deferred this. Prerequisites: (1) per-skill / per-turn FOUNDATION-mode state on agent_sessions or scratchpad, (2) a pre_tool hook insertion point that injects a maturity-floor predicate into outgoing tool inputs (search_knowledge.source_types, query_kg's edge filters, retrieve_related's per-arm filters). Without (1), there's no signal for the gate to fire on; without (2), the gate can't actually filter EXPLORATORY rows out of tool outputs. Both need a small design pass before implementation.
- [agent-claw/source-cache] thread the user's project context through services/agent-claw/src/core/hooks/source-cache.ts so the source_fact_observed events carry a real `group_id` instead of defaulting to '__system__'. The cache is currently shared across tenants — acceptable transitional state because source data is itself project-shared in many real workflows; revisit when project-tagged source data lands.
- [projectors/kg_documents] add a structured-extraction layer that turns chunk text into :Fact nodes pointing at their :Chunk via :DERIVED_FROM. Tranche 5 ships only the foundational chain (Document → HAS_CHUNK → Chunk); ChemDataExtractor / arrow-pushing extraction integrations land separately. Once shipped, query_provenance can return a real chain[] field walking Fact → Chunk → Document.
- [ingestion/doc_ingester] set `documents.metadata.group_id` from the ingesting user's project context so kg_documents inherits a real tenant scope. Today every document projection lands at `'__system__'` because the upstream importer doesn't tag — kg_documents' tenant-scope code path is dormant in production until this is wired.
- [projectors/kg_documents] convert the per-chunk Cypher loop to a single UNWIND-based MERGE so a 1000-chunk literature document doesn't need 1000+ Bolt round-trips. Current loop is correct but slow at scale.
- [logging/redactor-py] residual SMILES over-fire on multi-slash strings (e.g. `/path/to/file.txt`, `a/b/c/d/e/f`) — A12 cycle-4 tightening eliminated the ``opt=value`` / ``page=12`` class by restricting bond-target to SMILES atom letters but multi-slash file paths still match. Acceptable today (over-redacts harmless paths, never leaks); revisit if redactor counts spike on non-chemistry workloads
- [db/schema] pgvector 0.8 caps both ivfflat and hnsw at 2000 dims; canonical 2048-dim DRFP/Morgan columns on `reactions` + `compounds` are now un-indexed at the canonical layer (search routed through projector halfvec(2048) collections). Re-evaluate when pgvector lifts the cap or migrate to halfvec/bit indexing
- [db/init] replace lex-ordered `db/init/*.sql` with a real migration tool — fresh-DB smoke caught 3 schema bootstrap bugs (ivfflat dim, int2vector cast, missing column rename) that a migration runner with a CI smoke would have caught at PR time
- [workflow-engine] `_succeed` / `_fail` / `_maybe_retry` open a fresh psycopg connection per terminal write — short-lived but a per-worker long-lived write connection would be cleaner
- [workflow-engine] tighten `WorkflowDefinition` Zod to reject `conditional`/`loop`/`parallel`/`sub_agent` at define time once `_exec_<kind>` handlers ship; today the engine raises `NotImplementedError` and the validator accepts (documented contract)
- [audit-log] add a DROP-old-partition retention job alongside `audit_partition_maintainer` (configurable retention months)
- [service-registry] introduce `services/agent-claw/src/config/services.ts` + codegen for SERVICE_SCOPES (TS+Py), `config.ts` URL defaults, helm values, `.env.example` — kills the merge-debt bug class that produced ≥9 "missing X for service Y" landings in Wave 1
- [config-prove-out] migrate one feature flag to `isFeatureEnabled` to prove that infra path end-to-end (W2.9 finding — config_registry path proved out by promoter thresholds in PR #112; feature_flags path still has no production consumer)
- [tests/coverage] add a real-Postgres testcontainer test for the kg_source_cache → projector loop so the UUID-cast class of bug surfaces at PR time
- [tests/coverage] replace `# pragma: no cover` shims on optimizer/projector entrypoints with real smoke tests that import the module + assert configure_logging is wired (5 files: forged_tool_validator, session_purger, session_reanimator, kg_hypotheses, plus the deferred queue worker backoff calc and mcp_yield_baseline auth_headers fan-out — these are currently pragma'd to keep the diff-cover gate honest while the modules' main loops await testcontainer integration tests)
- [tests/integration] mcp_yield_baseline auth_headers integration: small unit test mocking httpx.Client to assert Authorization header is present on /tools/compute_drfp and /predict_yield calls (currently pragma'd; locks the DR-12 Bearer-auth fix)
- [db/init] schema_version provenance is inconsistent: only 8 of 46 `db/init/*.sql` files INSERT INTO schema_version. Either populate all files (or backfill the loader to record the filename automatically) so `SELECT * FROM schema_version` is the canonical applied-migrations list per CLAUDE.md
- [agent-claw/tests] add direct unit tests for `services/agent-claw/src/routes/optimizer.ts` and `services/agent-claw/src/routes/forged-tools.ts` — both routes are wired in `bootstrap/routes.ts` (lines 71/89) but have no `tests/unit/*-route.test.ts` covering the registered endpoints
- [agent-claw/optim] recommend_next_batch — etag-protect campaign+rounds across the two-transaction window so a concurrent ingest_campaign_results between read and write doesn't surface a stale-snapshot surrogate to the BoFire strategy
- [tests/coverage] add tests for `services/workflow_engine/main.py` (currently excluded from diff-cover; A09 added ingestion_events emit + _poll_conn lazy connection + dotted-path rename; testcontainer round-trip would lock the workflow_run_succeeded → projector materialisation contract)
- [DONE cluster-C] compound_class_assignments partial unique index `(inchikey, class_id) WHERE valid_to IS NULL` lands in 47_compound_class_assignments_unique.sql, with a dedupe pre-step that closes pre-existing duplicates by setting valid_to = NOW().
- [tests/coverage] extend `services/litellm_redactor/dynamic_patterns.py` test coverage for the new general unbounded-quantifier scanner (`(a+)+`, `(a|a)*`, `[a-z]+` rejection paths); module is currently in the [tool.coverage.run] omit list as a CI gate workaround
- [tests/coverage] add direct httpx-mocked test for `services/mcp_tools/mcp_doc_fetcher/fetchers.py` socket pin contextmanager — the DNS-rebinding TOCTOU mitigation lives in a thread-local; the BACKLOG'd doc-fetcher test re-enable should cover both surfaces
- [tests/python] `tests/unit/test_mcp_tabicl_*.py` (4 failures: featurize_happy_path, predict_and_rank_uses_inference, row_cap_rejection) regressed against `services/mcp_tools/mcp_tabicl/featurize.py` (X.shape mismatch — featurizer returning empty rows). Pre-existing; not part of Wave-2 scope
- [tests/python] `tests/unit/optimizer/test_gepa_*.py` — 3 collection errors importing services.optimizer.gepa_runner (likely missing optional dep in dev .venv); reproduce in CI before fixing
- [queue/worker] consolidate `_succeed`/`_fail`/`_maybe_retry` onto the existing `work_conn` instead of opening a fresh `psycopg.AsyncConnection` per terminal write — three connection opens per task under sustained load. NOTE: investigation in PR #108 found this is non-trivial — `work_conn` is held by the dispatch loop while concurrent `_handle` invocations (under `queue_concurrency`) can't share a single connection without per-call transaction conflicts. Either move to a small per-worker connection pool or accept the per-call open.
- [agent-claw/with-retry] retry-budget redesign: `withRetry` defaults are per-call (3 attempts, 5s cap); a downstream MCP saturating its limit can still amplify into a 9-attempt cascade across `findSimilar → expandReactionContext → predictYieldUq` chains. Introduce a per-request budget shared through the AsyncLocalStorage RequestContext so an upstream client gets bounded amplification regardless of fan-out depth
- [mcp_tools/common/app] add a circuit breaker around per-route handlers — current model retries every transient failure to its budget. Trip after N consecutive failures and short-circuit subsequent calls until the cooldown elapses; pair with a `/healthz` degraded indicator
- [observability/error_events] no production caller invokes `record_error_event` today (only the SQL trigger's own EXCEPTION-block forwarding does). Backfill is a fleet-wide rollout — every TS / Python error envelope writer needs to call the function before / instead of returning the JSON. Tracked as A20 follow-up
- [security/mcp-auth] implement `MCP_AUTH_SIGNING_KEY_NEXT` dual-key support in `services/mcp_tools/common/auth.py:_verify_token` (and the matching agent-side mint preference) so `MCP_AUTH_SIGNING_KEY` rotation can land without a brief 401 window. Runbook (`docs/runbooks/rotate-mcp-auth-key.md`) currently documents single-key rotation with maintenance-window 401s as the only supported flow.
- [DONE round-1] forge_tool TOCTOU: `tools` INSERT no longer uses `ON CONFLICT DO NOTHING`; unique-violation throws and rolls back the surrounding `withUserContext` transaction (forge_tool.ts:460-474).
- [DONE cluster-A] plan-mode persistTurnState now in finally block — runs on success, error, AND cancellation (routes/plan.ts).
- [DONE prior] SSE trackConnection has `error` listener at sse.ts:108 (verified during cluster-A audit).
- [agent-claw/runtime/monty] support `external_function("ask_user", ...)` via Monty snapshot/resume: bridge throws `AwaitingUserInputError`, harness suspends, script state dumps to `agent_sessions.scratchpad`, next user turn re-loads. Out of scope for v1 — `ask_user` is in the FORBIDDEN_TOOL_IDS list and preflight rejects it.
- [agent-claw/runtime/monty] ship a Langfuse dashboard config (`infra/langfuse/dashboards/code_mode_vs_react.json` or similar) that filters on `monty.run_id` / `monty.outcome` / `monty.external_calls_count` so operators can read p50/p95 wall-clock + cost in production. Telemetry already lands on the spans; the dashboard JSON is the missing artefact.

# Production-readiness round 1 (2026-05-07)
# Branch claude/prod-hardening-round-1 lands the BLOCKER + low-risk HIGH
# findings from the 5-agent audit. The items below are deferred — most
# need a dedicated branch with deeper investigation, broader refactor,
# or new infrastructure.

- [PARTIAL cluster-I] DynamicPatternLoader TTL knocked down 60s → 5s so admin-redaction mutations propagate within seconds rather than minutes. Reason: the proper fix (a cross-service /internal/redactor/invalidate endpoint) is more invasive and BACKLOG'd separately; the TTL drop is the cheap mitigation that doesn't require a new cross-service auth surface.
- [DONE deferred-I2] redaction_patterns now emits `NOTIFY redaction_patterns_changed '<scope>:<scope_id>'` on every INSERT/UPDATE/DELETE via db/init/50_redaction_patterns_notify.sql. services/litellm_redactor/listener.py provides an opt-in asyncio task that LISTENs on the channel and calls DynamicPatternLoader.invalidate() per NOTIFY. The litellm container is commented out in docker-compose.yml; when an operator uncomments it, they wire run_listener / start_background_listener via the litellm Python entrypoint. The 5s TTL fallback (cluster-I) remains as the operational safety net for offline-listener scenarios. Sidecar HTTP /internal endpoint approach was abandoned because the litellm_redactor runs as an in-process LiteLLM callback, not a separate sidecar — the pg_notify-channel approach uses existing infra and works across N replica processes without new auth surface.
- [agent-claw/permissions] `PolicyMatchContext` callers omit `org` / `project` fields, and `enforce`-mode default is "no policy match → allow" — combined, an org-scoped deny silently fails to fire. Fix: make org/project mandatory on PolicyMatchContext; thread them through resolver.ts and the permission_request lifecycle dispatch; treat missing as null.
- [agent-claw/db/with-system-context] `__system__` sentinel is implicit. A future owner-RLS policy that compares `user_entra_id = current_setting(...)` would silently match any row whose owner column is literally '__system__'. Fix: add CHECK constraints to every owner column rejecting that literal, plus an integration test that runs withSystemContext against feedback_events / corrections / notifications and asserts zero rows.
- [DONE deferred-D2] session_reanimator dev-mode x-user-entra-id header fallback removed; resume_session refuses with structured `mcp-auth-signing-key-required` envelope when MCP_AUTH_SIGNING_KEY is unset. Identity-spoof surface (MCP_AUTH_DEV_MODE leak → daemon resumes any user's session by stamping the header) is closed. Test class flipped to lock the new fail-closed contract. Same diff-cover fnmatch fix as deferred-D1 (omit at coverage.run layer).
- [DONE deferred-D3] BaseProjector._assert_bypass_rls runs at the top of _connect_and_run before catch_up. Refuses to start when the connected role lacks rolbypassrls (catches the env-var-override misconfig where POSTGRES_USER lands on chemclaw_app instead of chemclaw_service — FORCE RLS would otherwise silently drop every projector INSERT). Implements both the BACKLOG-flagged options (a) AND (b): a `enforce_bypass_rls_check` config flag (default true, escape hatch via PROJECTOR_ENFORCE_BYPASS_RLS_CHECK=false), AND a try/except that downgrades to WARN if pg_roles is unreadable / returns no row (test mocks, restricted role). 5 unit tests cover the matrix.
- [DONE cluster-B] permission_policies.argument_pattern is pre-compiled at refreshIfStale time; broken patterns log a structured WARN once per refresh cycle and are exposed via `brokenPolicyCount()` / `brokenPolicyIds()` so an admin endpoint can surface them. match() reads pre-compiled regex, no per-call try/catch.
- [DONE cluster-I] redactor 5MB ceiling now fails CLOSED — returns `[REDACTED:OVERSIZE]` placeholder + bumps the `oversize` count metric so operators can chart the rate. Pre-fix the function returned the unredacted text, which let any caller crafting a >5 MB payload exfiltrate raw SMILES / compound codes / emails through LiteLLM with the redactor silently passing them through. Chunk-and-redact for legitimate large payloads is a separate BACKLOG item.
- [DONE cluster-B] AGENT_ADMIN_USERS casing audit: bootstrap walks env entries vs admin_roles and WARNs on case drift (auditAgentAdminUsersCasing in middleware/require-admin.ts; wired in start.ts).
- [DONE cluster-H] bridge.ts routeExternalCall now stamps `monty.outcome="error"` and `monty.external_call.ok=false` on the span before rethrowing if anything between the registry lookup and `_routeExternalCall` completion throws (registry get throwing, lifecycle dispatch rejecting outside the inner try/catch). Pre-fix, a registry throw closed the span with no `monty.outcome` attribute; post-mortem trace queries by run_id missed the failure entirely. Test pins the rethrow + ensures the span is closed via the finally block.
- [DONE deferred-D1] queue worker drain landed: `Worker.run` now awaits in-flight `_handle_with_sema` tasks via `asyncio.gather(*self._inflight_tasks, return_exceptions=True)` bounded by new `queue_drain_timeout_s` setting (default 30s, fits k8s default `terminationGracePeriodSeconds`). Timed-out tasks are cancelled — their cancellation handlers release the lease via `_fail` so the row reverts to 'pending' rather than waiting QUEUE_LEASE_SECONDS for expiry. `_build_handlers` now returns `(handlers, aclose)` so the shared httpx client's connection pool gets closed cleanly on SIGTERM (pre-fix this leaked TCP slots on every replica restart). Tests updated to unpack the tuple.
- [DONE cluster-A] step.ts streaming text path now sums the streamCompletion finish-chunk usage alongside the call() usage (no more 50% undercount).
- [DONE cluster-A] step.ts parallel batch uses Promise.allSettled; post_tool_batch fires with sibling outputs + a `tool_execution_failed` envelope on the failed slot, then re-throws the first rejection.
- [DONE deferred-reanimator-lock] When tryIncrementAutoResumeCount returns null, the route now calls a new classifyAutoResumeFailure helper that checks if updated_at moved within the last 30s. If yes → 409 `session_in_progress` (the loser of a concurrent client+reanimator race; reanimator should retry next tick). If no → 409 `auto_resume_cap_reached` as before (genuine cap exhaustion). Implements BACKLOG option (B) "surface precise error" rather than the more invasive option (A) "advisory lock on every harness turn-runner" — the heuristic correctly distinguishes the two cases for observability without requiring chat.ts/plan/sub-agent to take the same lock.
- [DONE cluster-C] workflow_engine `_append_event` now takes a transaction-scoped `pg_advisory_xact_lock(hashtext('workflow_events'), hashtext(run_id::text))` before the MAX(seq)+1 INSERT, serialising writes for the same run across replicas.
- [DONE cluster-A] Pool slow-path now waits for the cold child's ready frame (bounded by `readyTimeoutMs`) and wraps in PrewarmedChildWrapper, so host's READY_TIMEOUT_MS isn't tripped on cold spawn.
- [DONE cluster-A] /api/chat/plan/approve persistTurnState moved into finally — runs on success/error/cancellation.
- [DONE cluster-A] Monty bridge span now emits canonical `tool.id` / `tool.read_only` / `tool.in_batch` alongside `monty.*` attrs; dashboards keyed on `tool.id` find Monty calls without special-casing.
- [observability/error_events] `record_error_event` SQL function has zero callers. The new Monty error outcomes (child_crashed, wall-time timeout, ready-frame timeout) are exactly what the table was designed for. Fix: wire `errors/envelope.ts:toEnvelope` to call `record_error_event` for top-level errors that surface to clients (start with route catch-all + Monty host finish path with `kind: "error"|"timeout"|"child_crashed"`).
- [DONE cluster-G] Monty runtime runbook landed at `docs/runbooks/monty-enable-and-rollback.md` covering enable order, per-tenant scoping, resource caps, kill-switch (per-tenant + global + pool drain), error codes to watch in Loki / `error_events`, and post-rollback restoration. CLAUDE.md runbook list reconciled to enumerate all 11 runbooks (was 6).
- [DONE cluster-B] admin_audit_log gained nullable request_id + trace_id columns (45_admin_audit_log_correlation.sql); appendAudit auto-populates from RequestContext + active OTel span.
- [DONE cluster-B] start.ts now refuses to boot when hookResult.skipped.length > 0, so a YAML added without matching BUILTIN_REGISTRARS entry fails loud at boot rather than silently disabling.
- [DONE cluster-H] user_hash._get_salt now gates on CHEMCLAW_DEV_MODE only (matching TS side); MCP_AUTH_DEV_MODE / PYTEST_CURRENT_TEST no longer enable the dev salt. Cross-service hash correlations now mismatch loud (TS raises, Python also raises) instead of silently disagreeing. services/mcp_tools/conftest.py sets CHEMCLAW_DEV_MODE=true so test runs that don't pre-set LOG_USER_SALT still work. New regression test pins the new gate exactly.
- [DONE cluster-B] initTracer now emits a structured WARN when no OTLP endpoint is configured AND CHEMCLAW_DEV_MODE !== "true" — silent span-drop in production now surfaces in Loki at boot.
- [DONE cluster-G] docker-compose.yml Langfuse port bound to `${LANGFUSE_BIND:-127.0.0.1}:${LANGFUSE_PORT:-3000}:3000` matching the LOKI_BIND / GRAFANA_BIND pattern. Pre-fix it was bound to all interfaces, leaking the dev API keys to anything reachable on the host network. Operators expose by setting LANGFUSE_BIND=0.0.0.0 explicitly when fronting with an SSO proxy.
- [DONE cluster-E] session_reanimator unit tests landed at `tests/unit/optimizer/test_session_reanimator.py` (19 tests): Settings posture + assert_production_safe four-state matrix, find_resumable SQL contract (6 invariants), JWT mint with scope=agent:resume + aud=agent-claw + ttl=300, dev-mode x-user-entra-id fallback (locks pre-cluster-D-revert behaviour), 409/5xx response handling, per-call x-request-id uniqueness. Stubs psycopg + httpx via AsyncMock; no testcontainer dep.
- [DONE cluster-E] audit_partition_maintainer unit tests landed at `tests/unit/optimizer/test_audit_partition_maintainer.py` (6 tests): Settings posture (BYPASSRLS role, 24h cadence, 3-month headroom), DSN assembly, _ensure_partitions calls `ensure_audit_log_partitions(months_ahead)` with the right parameter shape (no SQL injection surface), commit-on-success, NULL-row tolerance, int-coercion of string env settings. Stubs psycopg via AsyncMock.
- [tests/integration] All four permissionMode values exercised end-to-end through runHarness (only `enforce` is today). Especially the `plan` (`defer`) mode that `/api/chat/plan/approve` depends on.
- [tests/integration] Projector replay test parametrised over every projector (chunk_embedder, contextual_chunker, reaction_vectorizer, kg_documents, kg_experiments, kg_source_cache, compound_classifier, compound_fingerprinter, qm_kg, conditions_normalizer). DELETE FROM projection_acks → assert byte-identical re-derived state. Recovery contract from CLAUDE.md is fictional without this.
- [DONE cluster-E] real-Monty backend `_run_via_monty` unit tests landed at `tests/unit/test_monty_runner.py` (7 tests): _try_import_monty's None / sys.modules-stub branches, _run_via_monty's call shape (Monty(script).run(inputs=..., external_functions={"external_function": fn})), expected_outputs harvesting, _select_backend's monty-preferred / exec-fallback / refuse precedence. Loads the runner via importlib.util so the script can be exercised without restructuring the file.
- [tests/integration] forge_tool / promote_workflow_to_tool TOCTOU orphan: assert two concurrent forge_tool calls yield either both rows or neither — never skill_library without tools.
- [tests/integration] Lifecycle hooks `subagent_start/stop`, `task_created/completed`, `post_compact`, `session_end`, `user_prompt_submit`, `post_tool_batch` dispatched but not asserted in any integrated path. Extend `extended-hooks-fire.test.ts` to cover them.
- [tests/integration] Concurrent reanimator + client resume on the same session_id: `Promise.all([resume, resume])` at cap-1, assert exactly one succeeds.
- [tests/integration] Bi-temporal triple matrix: row valid now, row valid_to past, row invalidated_at past but valid_to future — query at three instants, assert membership.
- [tests/integration] Concurrent withUserContext: two parallel calls against the same RLS-protected table assert each user sees only their own rows. The single primitive standing between multi-tenant agent and cross-user data leakage.
- [DONE cluster-C] SECURITY DEFINER `bootstrap_config_setting` and `bootstrap_feature_flag` helpers added in 22_admin_rls_bootstrap_helpers.sql. 22_feature_flags.sql and 40_monty_config_seeds.sql now call them; ALTER FUNCTION OWNER TO chemclaw_service (BYPASSRLS) when role exists so SECURITY DEFINER actually bypasses FORCE RLS for non-superuser migration callers.
- [agent-claw/reactions] All reaction-similarity / applicability-domain / statistical-analyze / yield-prediction call sites (find_similar_reactions, predict_yield_with_uq, assess_applicability_domain, expand_reaction_context, statistical_analyze; also the Python `build_global_xgb.py` / `build_drfp_stats.py` scripts) lack `AND r.invalidated IS NOT TRUE AND r.valid_to IS NULL`. Fix: expose a `reactions_current` view with the filter built in, update callers to read the view; trains-the-baseline scripts swap to the view.
- [DONE cluster-C] audit_row_change EXCEPTION block now routes through `record_error_event(...)` so the 16 KiB payload cap applies uniformly; future maintainers attaching to_jsonb(NEW) for diagnosis can't blow the table.
- [DONE cluster-C] task_queue gained `enqueued_by` column (DEFAULT current_setting GUC); new `task_queue_insert_self` INSERT policy with WITH CHECK enforces enqueued_by = current user. chemclaw_service (BYPASSRLS) workers unaffected. 46_task_queue_tenant_scope.sql.
- [db/init] `34_kg_group_id_constraint.sql` and several other 33-40 init files are inconsistent about self-recording into schema_version. Pick one convention (rely on Makefile loop OR every file self-records); reconcile.

# Deep-review hardening follow-ups (2026-05-08, PR #123)

- [DONE PR-123] reactions_current view (db/init/48_*.sql); migrated 8 call sites including the Python tabicl_pca_coldfit script.
- [DONE PR-123] compound_fingerprinter._fingerprint per-rule failure tracking + transaction rollback contract; closes the partial-state idempotency gap.
- [DONE PR-123] permission resolver enforce-mode no-policy default flipped allow → ask.
- [DONE PR-123] workflow_engine pg_advisory_lock per run_id + ConfigRegistry-driven http_timeout / wait_poll_interval / default_wait_timeout.
- [DONE PR-123] withUserContext passes captured error to client.release(err) so node-postgres destroys clients with possibly-open transaction state.
- [DONE PR-123] mock_eln + fake_logs FORCE ROW LEVEL SECURITY with a known-roles-only ALL policy on every table (db/init/49_*.sql).
- [tests/projectors] add `tests/unit/projectors/test_compound_fingerprinter_errors.py` mirroring the chunk_embedder error-policy pattern (httpx mock for `/tools/substructure_match` 4xx, assert PermanentHandlerError raised + work_conn.rollback() called); compound_fingerprinter currently sits in coverage.run omit alongside workflow_engine because the new failure path needs a testcontainer.
- [agent-claw/permissions] thread `org` / `project` through PolicyMatchContext for real (PolicyMatchContext already has the fields; ToolContext doesn't carry tenant binding yet — Phase F.3). Once threaded, the resolver enforce-mode default can stay "ask" without operators needing the global allow-all policy escape hatch.
- [tests/agent-claw] update permission-enforce-mode.test.ts to also assert the WARN log fires with `event: "permission_enforce_no_policy_match"` when the default fires (currently only the decision is asserted).
- [agent-claw/sse] throttle/coalesce `tool_log` SSE events — a chatty Monty/run_program script can flood the channel with thousands of lines (no backpressure). Either token-bucket per session or merge consecutive same-stream lines.
- [agent-claw/routes] add per-route rate-limit to `/api/internal/workflows/sub_agent` mirroring `/api/internal/sessions/:id/resume` once a sensible cap is chosen — defense-in-depth against a leaked engine signing key.
- [workflow_engine/sub_agent] support substring `${jmespath}` template substitution in `goal` (today only whole-string templates resolve — `look up ${steps.first.smiles}` passes through literally).
- [agent-claw/registry] forged-tool `_forgedCodeBySha` cache has no eviction; bounded only by distinct forged-tool count. Add LRU if forged-tool registries grow into the thousands.
- [infra/git-proxy] `git push origin --delete claude/<branch>` returns HTTP 403 from the in-process git proxy — leaves merged claude/* branches dangling on the remote (compare `gh api repos/.../branches`). Either grant the proxy delete permission for claude/* refs or document a manual janitor pass.
- [agent-claw/permissions] introduce `clearPermissionPolicyLoader()` so tests don't have to cast `setPermissionPolicyLoader(null as never)` (matrix test today does this in finally).
- [agent-claw/admin-routes] tighten the audit-meta-invariant regex from `\b\w+\.${verb}\(` to anchor on the actual Fastify decorator name to eliminate false-positive risk if a non-Fastify `*.delete(` pattern lands in `routes/admin/`.
